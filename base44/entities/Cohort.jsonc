{
  "name": "Cohort",
  "properties": {
    "course_id": {
      "type": "string"
    },
    "end_date": {
      "format": "date",
      "type": "string"
    },
    "institution_id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "start_date": {
      "format": "date",
      "type": "string"
    }
  },
  "required": [
    "name",
    "course_id",
    "start_date",
    "institution_id"
  ],
  "rls": {
    "create": {
      "$and": [
        {
          "data.institution_id": "{{user.data.institution_id}}"
        },
        {
          "user_condition": {
            "role": "academic"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "data.institution_id": "{{user.data.institution_id}}"
    },
    "update": {
      "$and": [
        {
          "data.institution_id": "{{user.data.institution_id}}"
        },
        {
          "user_condition": {
            "role": "academic"
          }
        }
      ]
    }
  },
  "type": "object"
}
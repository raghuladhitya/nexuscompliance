{
  "name": "Unit",
  "description": "A teaching unit within a course, scheduled unit-by-unit and day-by-day by Academics.",
  "properties": {
    "name": {
      "type": "string"
    },
    "code": {
      "type": "string"
    },
    "course_id": {
      "type": "string"
    },
    "start_date": {
      "format": "date",
      "type": "string"
    },
    "end_date": {
      "format": "date",
      "type": "string"
    },
    "institution_id": {
      "type": "string"
    }
  },
  "required": [
    "name",
    "course_id",
    "institution_id"
  ],
  "rls": {
    "create": {
      "$and": [
        {
          "data.institution_id": "{{user.data.institution_id}}"
        },
        {
          "$or": [
            {
              "user_condition": {
                "role": "academic"
              }
            },
            {
              "user_condition": {
                "role": "admin"
              }
            }
          ]
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "data.institution_id": "{{user.data.institution_id}}"
    },
    "update": {
      "$and": [
        {
          "data.institution_id": "{{user.data.institution_id}}"
        },
        {
          "$or": [
            {
              "user_condition": {
                "role": "academic"
              }
            },
            {
              "user_condition": {
                "role": "admin"
              }
            }
          ]
        }
      ]
    }
  },
  "type": "object"
}